apiVersion: v1
kind: Service
metadata:
  name: localserve-frontend-service
spec:
  selector:
    app: localserve-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
