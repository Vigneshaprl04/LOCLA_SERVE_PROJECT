apiVersion: v1
kind: Service
metadata:
  name: localserve-backend-service
spec:
  selector:
    app: localserve-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
